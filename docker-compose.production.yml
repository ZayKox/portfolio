name: portfolio-production

services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        SITE_URL: ${SITE_URL:?Set SITE_URL to the final HTTPS origin}
    restart: unless-stopped
    expose:
      - "8080"
